/**
 * Home Page Renderers
 * Code mới - đơn giản và trực tiếp
 */

export function renderHomeNovels(novels) {
    // This function is kept for compatibility but the actual rendering
    // is now done in pages/home.js for simplicity
    console.log('[Home Renderer] renderHomeNovels called (delegated to home.js)');
}
